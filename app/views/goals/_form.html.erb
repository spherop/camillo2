<%= simple_form_for @goal  do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :title, placeholder: "Goal Title" %>
    <%= f.text_area :notes, class: "rte" %>
  </div>

  <div>
    <%= f.button :submit %>
  </div>
<% end %>
